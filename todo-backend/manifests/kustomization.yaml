apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# secret.enc.yaml is encrypted with SOPS and applied separately
resources:
  - configmap.yaml
  - postgres.yaml
  - deployment.yaml
  - cronjob.yaml
