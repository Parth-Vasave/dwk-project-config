# Argo CD keeps the production namespace in sync with project/overlays/production on the main branch
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: project-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Parth-Vasave/dwk-project-config
    targetRevision: main
    path: project/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    # Apply every new commit automatically, delete resources removed from Git and undo manual changes in the cluster
    automated:
      prune: true
      selfHeal: true
