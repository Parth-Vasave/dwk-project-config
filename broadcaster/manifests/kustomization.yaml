apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# secret.enc.yaml (the Discord webhook URL) is encrypted with SOPS and only applied to the main environment
resources:
  - nats.yaml
  - deployment.yaml
